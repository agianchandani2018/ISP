import requests
import json



def getNumberOfCommits(username, repoName):


def getDatesOfCommits(username, repoName):


def numCommitsDateRange(username, repoName, startDate, endDate):

# returns a stirng that indicates if the student is up to date, behind, etc
def statusMessage()
